package com.muhammadshandy.listfilmuts.uas.utils

import com.muhammadshandy.listfilmuts.BuildConfig

/**
 * Object untuk menyimpan constant yang diperlukan
 */
object Constants {
    /**
     * Constant dari Base URL yang digunakan
     */
    const val BASE_URL = BuildConfig.BASE_URL

    /**
     * Constant dari Image URL yang digunakan (Untuk mendapatkan gambar)
     */
    const val IMG_URL = BuildConfig.IMG_URL

    /**
     * Api Key yang digunakan
     */
    const val API_KEY = BuildConfig.API_KEY
}